import React from 'react';
import withAuthorized from "../../hocs/Authorized";

const Users = () => {
    return (
        <section className="users">

        </section>
    );
};

export default withAuthorized(Users);